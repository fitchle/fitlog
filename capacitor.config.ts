import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fitchle.fitlog',
  appName: 'fitlog',
  webDir: 'out',
  bundledWebRuntime: false,
  android: {
    
  }
};

export default config;
